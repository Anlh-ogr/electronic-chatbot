# FastAPI runtime
APP_NAME=Electronic Chatbot API
ENV=dev
HOST=0.0.0.0
PORT=8000

# CORS (frontend dev)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Data "database" (JSON converted from YAML)
DATA_PATH=app/data/circuits.json
